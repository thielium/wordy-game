{
  "name": "WordyGame",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently 'cd frontend; react-scripts start' 'cd backend; nodemon --config nodemon.json'"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2",
    "ts-node": "^8.8.1",
    "tslint": "^6.1.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "@types/yaml": "^1.2.0"
  }
}
